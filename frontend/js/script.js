/** Arquivo para armazenar as constantes do projeto */
var mid = 'http://localhost:3000/imagem';

function showToast (text) {
    M.toast({html: '<small>'+text+'</small>', classes: 'rounded', activationPercent: 0.1});
}